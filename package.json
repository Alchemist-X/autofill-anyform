{
  "name": "autofill-anyform",
  "version": "2.0.0",
  "description": "MV3 Chrome extension that fills any web form from a saved profile using field heuristics, with an optional LLM second pass. Data stays local.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test",
    "build:zip": "node scripts/build-zip.mjs",
    "eval": "node eval/eval.mjs"
  }
}
